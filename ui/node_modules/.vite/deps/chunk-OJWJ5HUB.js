import {
  round
} from "./chunk-ELHZZCZA.js";
import {
  getComputedStyle,
  isHTMLElement
} from "./chunk-D3NO7VQ3.js";

// node_modules/.pnpm/@base-ui+react@1.6.0_@types_608ed341cf667a023708209f3df4deb1/node_modules/@base-ui/react/utils/getCssDimensions.mjs
function getCssDimensions(element) {
  const css = getComputedStyle(element);
  let width = parseFloat(css.width) || 0;
  let height = parseFloat(css.height) || 0;
  const hasOffset = isHTMLElement(element);
  const offsetWidth = hasOffset ? element.offsetWidth : width;
  const offsetHeight = hasOffset ? element.offsetHeight : height;
  const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
  if (shouldFallback) {
    width = offsetWidth;
    height = offsetHeight;
  }
  return {
    width,
    height
  };
}

export {
  getCssDimensions
};
//# sourceMappingURL=chunk-OJWJ5HUB.js.map
